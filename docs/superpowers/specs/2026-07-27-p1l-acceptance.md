# P1L 迭代A（预览/终端 chrome 清理 + tab 栏打磨）人工验收记录

**状态:待用户实机视觉验收（2026-07-27 代码完成:全量套件绿、clippy/fmt 干净;4 task 各过 task 级审阅 + opus 终审 GO;+T5 验收前返工,补齐预览侧同款处理）。**
**验收人:用户(甲方);验收权归用户,实施方不代签。**
**重要:本迭代纯视觉/交互,headless 全程未验真机——必须用户实机逐项目测。**

分支 `feat/p1l-preview-terminal-chrome`（base b2e5497），5 提交:
- `6db393e` #7 Flyfish 工具栏关闭 · `d257946` #4 去头部标题+几何 · `040c730` #6 关闭×进框 · `51cf641` #5 tab 横滚 · `f552192` T5 横滚→箭头翻页(终端+预览统一,顺带去掉未定制灰滚动条)

## 清单逐项结果

| # | 反馈 | 验收项 | 结果 | 备注 |
|---|------|--------|------|------|
| 1 | #4 | 预览/终端两栏顶部不再有"预览·P1d"/"终端·本计划"标题,tab 栏成顶行 | 待验 | |
| 2 | #4 | 终端网格底部光标行完整可见、未被状态条遮挡(几何未错位) | 待验 | 终审重点核查过的几何点 |
| 3 | #4 | 打开文件预览,webview 与 iced 边框对齐(几何未错位) | 待验 | |
| 4 | #7 | 预览内容上方不再有"搜索/下载/打印/html"工具栏 | 待验 | Flyfish toolbar=false |
| 5 | #6 | 每个 tab 的关闭 × 落在该 tab 的矩形框内(激活 tab 有 CARD 底 pill,× 在其右侧同框),归属清晰 | 待验 | 终端+预览两处 |
| 6 | #5 | 开足够多 tab 超出栏宽时,左右箭头可翻页(到头变灰不可点);`＋`/"打开文件…"始终常驻右侧可点、不被挤走/翻走 | 待验 | 终端+预览两处;T5 已把横向 scrollable 换成箭头翻页,原「滚动条未按主题定制」的取舍项随之消失 |

## 需你实机判定的视觉取舍（Task 3 引入,非 bug）

| 取舍 | 现状 | 说明 |
|------|------|------|
| A | 预览非激活 tab 由 PANEL 底+边框 → **透明**(与终端一致) | 统一成"同款 pill"的必然结果;active 仍靠 CARD 底 pill 突出。是否符合预期? |
| B | 终端非激活 tab 标题色 BODY(暗) → **CREAM**(亮) | active/非 active 现只靠 pill 底色区分,不再叠加文字明暗。可接受? |

## 反馈 → 修复记录(按轮次)

(待用户实机反馈后填写)

## 结论

(全部通过后填写)
