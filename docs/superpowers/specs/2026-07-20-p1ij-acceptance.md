# P1i + P1j 对话可审阅性人工验收记录

**状态:✅ 已验收通过(2026-07-20,用户实机;含启动扫描回归修复)。**
**验收人:用户(甲方);验收权归用户,实施方(agent)不代签。**
**范围:P1i(transcript 适配器 + 审阅 tab 引擎) + P1j(右一对话列表入口)合一验收——P1j 让 P1i 引擎产生用途,两者构成完整"对话可审阅性"。**

## 清单逐项结果(dogfooding 本仓)

| # | 验收项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 打开本仓 → 右一 AI 栏默认"对话"视图,列出历史对话(标题=首句、时间倒序、`claude·时间·规模`) | ✅ | 切 dozer 本仓列出 9 条 |
| 2 | Dozer 里跑 claude → 该会话对话在列表顶部标"● 当前"(金框高亮) | ✅ | |
| 3 | 点一条历史对话 → 左二审阅 tab 结构化呈现(人类锚点 ▎+ AI 回合折叠,▸过程展开) | ✅ | |
| 4 | 切"Agents"视图 → 占位;切回"对话" → 列表还在 | ✅ | |
| 5 | 当前 claude 再答一轮 → 回合结束:当前对话刷新、历史对话不动(File 源快照) | ✅ | |
| 6 | 终端 tab 不再有"审阅"按钮(入口统一到右一列表) | ✅ | |
| 7 | 非本项目 cwd/无记录 → 右一"暂无对话记录",不崩 | ✅ | test_math 空目录正确空列表 |
| 8 | 关 app 重开 → 打开项目后对话列表恢复 | ✅ | 见下修复轮次 |

## 反馈 → 修复记录(按轮次)

**轮次 1 — 启动恢复项目后对话列表空白(验收项 #8 暴露的回归)**
- 现象:重开 app,列表"列出失败"(恒空)。
- 根因:`Workspace::bootstrap` 启动恢复上次项目时只 `set self.project`,未像
  `ProjectOpened` 那样接线异步刷新;line 408 注释承诺"git 分支/脏窗口起来
  后异步补",但启动路径漏了 `spawn_conversations_refresh`(与 `spawn_project_git_refresh`)。
- 修复:构造末尾对已恢复项目补两个 refresh。实测启动扫 dozer 本仓 `n=9`(此前 0)。
- 提交:`4943e1d`。

## 设计沿革(供追溯)

- P1i 首版把审阅入口放终端 tab、只看当前会话,用户指出"与终端复读无异、无实质用途"。
- 结论:P1i 是引擎(transcript 适配器 + 审阅渲染),缺的是"翻阅非当前对话"的入口。
- P1j 提供入口:右一 AI 栏对话列表(扫 Claude 目录,活对话置顶),点任一条进审阅;收敛移除终端"审阅"按钮。
- 数据模型愿景 `agent ▸ session ▸ conversation` 三层;本切片落扁平一层(项目▸对话),`agent` 字段 + 对话来源抽象两处门为多 agent/两层分组留路(§8 索引推二期)。
- Figma 参考屏:node 71:2(会话列表)/ 71:183(审阅详情)。

## 结论

8/8 通过。P1i(审阅引擎)+ P1j(右一对话列表入口)构成完整"对话可审阅性",
合一交付。规格 §3 需求 6 追加达成、§7 AI 栏"含会话列表"修订已回填。分支
`feat/p1i-conversation-review`(含 P1i 6 提交 + P1j 4 提交 + 启动扫描修复 1 提交)
待合入 main。
