# P1h 文件树 git 装饰人工验收记录

**状态:待验收(2026-07-19 全量回归已绿:15 套件无失败、clippy 零警告、fmt 干净;app 冒烟 8 秒无 panic)。**
**验收人:用户(甲方);验收权归用户,实施方(agent)不代签。**

## 清单逐项结果(dogfooding 本仓)

| # | 验收项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 改一个已跟踪文件 → 树对应行金 + `•`,所在目录(折叠时)也金 `•` | 待验 | |
| 2 | 新建未跟踪文件 → 树对应行绿 + `+`(可见/展开处) | 待验 | |
| 3 | 删一个已跟踪文件 → 对应行(可见)红 + `−` | 待验 | |
| 4 | 让 claude 改文件 → 回合结束后装饰自动刷新(无需手动重开项目) | 待验 | |
| 5 | `git checkout`/提交清干净 → 下次回合结束或重开项目 → 装饰消失 | 待验 | |
| 6 | 打开非 git 目录 → 文件树无装饰、不崩 | 待验 | |

## 反馈 → 修复记录(按轮次)

(待验收反馈后填写)

## 结论

(全部通过后填写;规格 §7 追加 P1h 达成标注)
