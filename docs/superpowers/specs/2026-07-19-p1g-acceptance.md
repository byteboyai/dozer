# P1g 最小可用项目层人工验收记录

**状态:验收通过,已定稿(2026-07-19)。**
**验收人:用户(甲方);验收权归用户,实施方(agent)不代签。全部清单项经用户实机验证确认。**
**前置回归:cargo test 149 通过、clippy 零警告、fmt 干净;app 冒烟 8 秒无 panic。**

## 清单逐项结果(dogfooding 本仓)

| # | 验收项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 首启无项目 → 左一"打开项目…" + 最近列表 | ✓ 2026-07-19 | |
| 2 | 打开本仓为项目 → 项目名 + `main*`(脏时金*) + 路径 + 文件树(.git/target 不显示,目录在前) | ✓ 2026-07-19 | |
| 3 | 点目录 ▸ 展开/收起;点文件 → 左二 Flyfish 预览 | ✓ 2026-07-19 | |
| 4 | 新终端 tab(＋)→ 落在项目根,非 $HOME | ✓ 2026-07-19 | |
| 5 | `.dozer/goal.md` 定标 → claude 改文件 → 回合毕横幅(挂到项目仓库) | ✓ 2026-07-19 | |
| 6 | 打开第二个 git 仓库 → 项目名/分支/文件树跟随;最近列表可切回 | ✓ 2026-07-19 | 顺带催生切换清理(见反馈) |
| 7 | 关 app 重开 → 当前项目自动恢复 | ✓ 2026-07-19 | |
| 8 | 打开非 git 目录 → 文件树可用,分支 `—`,不崩 | ✓ 2026-07-19 | |

## 反馈 → 修复记录(按轮次)

**第一轮(切换项目未清理)**:打开第二个 git 仓库后,上一个项目遗留的预览文件 tab 与终端 tab 仍开着。
→ `4ccdd73` feat(项目):切到不同项目时关掉上一个项目遗留的终端 tab(=结束会话,同 CloseTab 语义)与预览 tab,给新项目干净起点;首次打开项目(无前项目)不强关。

## 结论

P1g 验收通过。规格 §7 左一项目栏(最小可用部分)达成:项目对象/持久化/当前项目/最近项目、项目卡(名称+git 分支脏)、懒加载文件树、点文件进预览、重锚终端与验收到当前项目、切换清理。
项目信息存 dozerd SQLite(`~/Library/Application Support/ai.byteboy.dozer/dozer.db` 的 `projects` + `meta` 两表),与验收记录同库。
已知边界(非缺陷,后续承接):H0 独立项目中心页、组件视图(dbx 二期)、doctor、文件树 git 装饰/增删改、.gitignore 精确过滤、多项目并列、文件树 spawn_blocking(超大目录)。
下一阶段起点:`feat/p1g-project-layer` 并入 main 后的工作区。会话级变更文件归因(P1f 第五轮反馈,hook PostToolUse 数据)仍为承接项之一。
