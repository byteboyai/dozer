# P1g 最小可用项目层人工验收记录

**状态:待验收(2026-07-19 全量回归已绿:149 测试通过、clippy 零警告、fmt 干净;app 冒烟 8 秒无 panic)。**
**验收人:用户(甲方);验收权归用户,实施方(agent)不代签。**

## 清单逐项结果(dogfooding 本仓)

| # | 验收项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 首启无项目 → 左一"打开项目…" + 最近列表(若有) | 待验 | |
| 2 | 打开本仓为项目 → 左一见项目名 + `main*`(脏时金*) + 路径 + 文件树(.git/target 不显示,目录在前) | 待验 | |
| 3 | 点目录 ▸ 展开/收起;点文件 → 左二 Flyfish 预览 | 待验 | |
| 4 | 新终端 tab(＋)→ 落在项目根(`pwd` 确认),非 $HOME | 待验 | |
| 5 | `.dozer/goal.md` 定标 → claude 改文件 → 回合毕横幅(验收挂到项目仓库,即使 tab 曾 cd 别处) | 待验 | |
| 6 | 打开第二个 git 仓库为项目 → 项目名/分支/文件树全跟随;最近列表可切回 | 待验 | |
| 7 | 关 app 重开 → 当前项目自动恢复 | 待验 | |
| 8 | 打开非 git 目录 → 文件树可用,分支区 `—`,不崩 | 待验 | |

## 反馈 → 修复记录(按轮次)

(待验收反馈后填写)

## 结论

(全部通过后填写;规格 §7 项目栏追加 P1g 达成标注)
