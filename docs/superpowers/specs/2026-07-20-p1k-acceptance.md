# P1k 外壳 chrome 还原度 人工验收记录

**状态:待用户实机验收（2026-07-20 代码完成:全量 15 套件绿、clippy/fmt 干净;5 task 各过 task 级审阅 + opus 整分支终审）。**
**验收人:用户(甲方);验收权归用户,实施方不代签。**
**重要:本迭代目标即"视觉还原度",而全程 headless 实现——所有真机视觉均未由实施方验证,必须用户实机逐项目测。**

分支 `feat/p1k-shell-chrome`（base 9b0eec0），提交链:
- `ba6b260` T1 顶栏 · `6416338` T2 状态栏 · `fef27cb` T3 树图标 · `79a4ab7`+`8fbef92` T4 卡片打磨(+1 修) · `7307d80`+`e08f923` T5 验收数RPC(+1 修) · `607b306` 终审几何修复

## 清单逐项结果(dogfooding:打开本仓 dozer)

| # | 验收项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 顶栏出现:左"Dozer"标题、中 ⌘K 搜索框(占位)、右金框"目标：…"胶囊 + 齿轮 | 待验 | 无项目时胶囊隐、搜索框在 |
| 2 | **⚙ 齿轮字形正常渲染**(非豆腐块/非 emoji 异常) | 待验 | 终审标注的字体毒化风险点,重点看 |
| 3 | 项目栏底状态条:绿点"环境正常 · dozerd 运行中" + [文件·git main·组件];杀 dozerd 显红"未连接" | 待验 | |
| 4 | 终端栏底状态条:agent 态点+中文 · resume · "dozerd 持有 · 断连可恢复" | 待验 | |
| 5 | 文件树:目录 ▾/▸ 三角、文件 · 前导;改文件行尾金●、新绿●、删红● | 待验 | 改本仓某文件观察 |
| 6 | 项目信息成圆角卡;有验收记录的项目显金色"N 次验收"副行 | 待验 | 本仓 dozer 有验收记录 |
| 7 | 激活 tab 有 CARD 底 pill 高亮、其余透明;交付横幅/会话卡圆角 | 待验 | |
| 8 | **终端网格底部正常**:最底行(含正在输入的光标行)完整可见,未被底部状态条遮挡/裁掉 | 待验 | 终审 Critical 修复点——填满屏幕后重点看最后一行 |
| 9 | **预览 webview 对齐**:打开文件/网页预览,webview 与 iced 边框/地址栏对齐,未上移盖住顶栏/未底部越界 | 待验 | 终审 Important 修复点 |
| 10 | 中文输入法候选窗落在光标右下(顶栏偏移已回灌),非窗口左上 | 待验 | IME 几何修复点 |

## 审阅经过(供追溯)

- 5 task 各过 sonnet task 级审阅(spec 符合 + 代码质量双裁决),T4/T5 各经一轮 Important 修复:
  - T4:去 card_col 跨任务 mut 桥接 + 非激活 tab 用默认透明边框。
  - T5:①bootstrap 补 spawn_acceptance_count_refresh(同款 startup-restore 回归);②count 查询键对齐 delivery::repo_root(抽 acceptance_query_repo + 非 git→None 测试),消除子目录/符号链接静默空白。
- **opus 整分支终审判 NO-GO**,逮到 5 次 task 级审阅漏掉的跨切片几何回归:顶栏 44px + 状态栏 26px 吃掉的高度未回灌 `terminal_pane_pixel_size`/`preview_content_bounds`/`ime_cursor_area` → 终端多算~3 行、光标行被状态条遮挡、预览错位。已修(抽 TOP_BAR_HEIGHT/STATUS_BAR_HEIGHT 常量,渲染点复用防漂移,三函数回灌 + 回归测试),复验全量绿。

## 反馈 → 修复记录(按轮次)

(待用户实机反馈后填写)

## 结论

(全部通过后填写;规格 §7 回填"顶栏/状态栏落地"标注)
